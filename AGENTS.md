# AGENTS.md

## 项目概述

- 本项目是前后端分离的系统：
    - `admin/`：前端项目，使用 Vue 3、Vite、JavaScript、Element Plus、Pinia。
    - `server/`：后端项目，使用 PHP 8.2+、Webman、Illuminate Database、Phinx。
    - `docs/`：项目资料与业务文档，除非任务明确要求，否则不要主动新增或改写。
- 开发目标是让代码能够由对应职能的开发人员独立理解和 Review。Agent 应优先采用项目已有目录、分层、组件、工具函数和代码风格，不得仅因个人偏好引入新的架构或实现方式。

## 任务执行规则

- 回复、注释、文档尽量使用中文
- 避免主动编写任务文档文件、示例、测试代码和执行测试命令，除非用户主动要求
- 输出任务总结尽量简洁，重要对话中不应该包含无关紧要的信息
- 实现新的功能时尽可能参考现有代码，保证代码风格一致且保证代码复用性。
- 修改前先阅读目标文件及其直接调用方；不得只根据文件名或局部代码猜测业务行为
- 未经用户明确要求，不主动执行依赖升级、全量格式化、构建、发布、数据库迁移或其他可能改变工作区及运行环境的命令
- 每次任务执行完成如果修改了代码，需要总结一个 git message 文本，只需要一句概要描述，如 `feat|fix|docs|...(order): 修复订单查询异常`
- 优化和重构时不要将原有的注释精简，保持注释的可读性，并且新增代码中复杂的逻辑需要添加注释说明。

## 数据库迁移规范

- 表结构迁移和必需初始化数据迁移应分离；结构迁移只负责建表、索引、外键等 schema 变更。
- 初始化数据迁移只写入框架运行必需数据，并保证幂等、可回滚、按模块分组清晰。
- 迁移中应尽量使用 Phinx 抽象能力，减少数据库方言硬编码；必须使用特定数据库语法时要明确限制范围。
